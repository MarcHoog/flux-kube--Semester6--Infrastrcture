apiVersion: v1
kind: Secret
metadata:
  name: system-operator-secret
  namespace: kube-dashboard
  annotations:
    kubernetes.io/service-account.name: system-operator
type: kubernetes.io/service-account-token